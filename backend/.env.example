# Server
PORT=5000

# Databases
DATABASE_URL=postgresql://user:password@localhost:5432/cricbid
MONGO_DB_URI=mongodb://127.0.0.1:27017/auctioner

# WhatsApp (Meta Graph API)
META_API_KEY=

# --- Daily activity summary email ---
# SMTP credentials used to send the daily "user_event" activity summary email.
# For Gmail: host=smtp.gmail.com, port=587, user=<gmail address>,
# pass=<Google App Password> (not your regular password).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Comma-separated list of recipients for the daily activity summary email.
DAILY_SUMMARY_RECIPIENTS=prajyotsancheti177@gmail.com
