# Server
PORT=5000
NODE_ENV=development

# Postgres (Prisma)
DATABASE_URL=postgresql://user:password@localhost:5432/cricbid

# Legacy MongoDB
MONGO_DB_URI=mongodb://127.0.0.1:27017/auctioner

# WhatsApp / Meta Cloud API
META_API_KEY=

# Google Sheets/Drive sync
GOOGLE_SERVICE_EMAIL=
GOOGLE_PRIVATE_KEY=

# AWS S3 uploads
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_BUCKET_NAME=

# Daily activity report email (SMTP)
# For Gmail: host smtp.gmail.com, port 587, and an app password (not your login password)
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
REPORT_RECIPIENT_EMAIL=prajyotsancheti177@gmail.com
